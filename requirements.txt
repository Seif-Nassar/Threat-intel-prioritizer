# ── Core ML & data ────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.4.0
joblib>=1.3.0

# ── Deep learning (CodeBERT embeddings) ───────────────────────────────────────
# For GPU (CUDA 11.8):  pip install torch --index-url https://download.pytorch.org/whl/cu118
# For GPU (CUDA 12.1):  pip install torch --index-url https://download.pytorch.org/whl/cu121
# For CPU only (slower): the line below is sufficient
torch>=2.2.0
transformers>=4.40.0

# ── Web UI ─────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0

# ── Data ingestion ────────────────────────────────────────────────────────────
nvdlib>=0.7.6
requests>=2.31.0

# ── Schemas & utilities ───────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0
